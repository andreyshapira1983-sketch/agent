# Модуль для интеграции RESTful API
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/api/v1/resource', methods=['GET'])
def get_resource():
    return jsonify({'message': 'Resource data'})

if __name__ == '__main__':
    app.run(debug=True)