# Архитектурная перепись файлов

Инвентаризация фактических ответственностей. Не рефакторинг: код в ходе анализа
не менялся, перенос и переименование не начинаются до отдельного подтверждения
оператора и до окончания инвентаризации всего выбранного слоя.

**Дата начала:** 2026-08-05. **Слой этого прохода:** `core/loop*.py` — 20 файлов,
7428 строк.

## Метод

Машинные факты (символы, диапазоны строк, входящие и исходящие зависимости,
маркеры побочных эффектов) извлечены разбором AST по всему репозиторию, а не по
памяти. Суждения — от чтения файлов. Там, где файл ещё не прочитан целиком, это
сказано прямо, и выводы помечены как **гипотезы**.

Два правила, которые пришлось применять против собственного первого впечатления:

- **Размер не диагноз.** `loop_sensor.py` — 44 строки и связен; `loop_init.py` —
  301 строка и, по-видимому, тоже. Ниже классифицируются не объёмы.
- **Имя не доказательство.** `loop_attempt.py` называется «цикл попыток», а
  владеет ещё и добычей плана пятью разными путями. Имя проверяется телом.

---

## Находки уровня слоя

### Л1. Двадцать файлов, но не двадцать модулей

Из 19 модулей `loop_*.py` **у каждого ровно один импортёр — `core/loop.py`**.
Ни один не используется ничем другим: ни другим модулем `core/`, ни `cli/`, ни
`app/`, ни напрямую тестами (кроме тестов самого раскола).

```
loop_attempt.py           <- core/loop.py
loop_step_execution.py    <- core/loop.py
loop_synthesis.py         <- core/loop.py
…и так все 19
core/loop.py              <- 29 файлов вне core/ (app/, cli/, scripts/)
```

Это примеси (mixins), собираемые в один класс `AgentLoop` (`core/loop.py:143`).
Раскол дал **границы файлов, но не границы модулей**: между кусками нет
интерфейса, каждый обращается к `self.` за состоянием, которым владеет соседний
кусок. Проверяемое доказательство — блок объявлений в
`core/loop_attempt.py:120-140`, где перечислены 13 атрибутов, которые файл
использует и не создаёт, с комментарием в самом коде:

> «Берётся у соседней примеси: работает через MRO, но связь между модулями
> обязана быть записана, иначе её видно только на прогоне.»

**Следствие для переписи:** ни один из 19 файлов нельзя классифицировать как
`cohesive` на основании того, что он компилируется отдельно. Единица связности
здесь — класс `AgentLoop`, а не файл.

### Л2. Три метода держат пятую часть слоя

| метод | файл:строки | строк |
|---|---|---|
| `AgentLoop._run_inner` | `core/loop.py:276-692` | **417** |
| `AgentLoopAttempt._run_attempt_loop` | `core/loop_attempt.py:142-564` | **423** |
| `AgentLoopStepExecution._execute_step` | `core/loop_step_execution.py:212-769` | **558** |

1398 строк, 18.8 % слоя, в трёх функциях. Раскол на 20 файлов перенёс массу, но
эти три тела не тронул: каждое переехало целиком. Отсюда прямой ответ на вопрос,
почему половина жильцов прописана в `loop_attempt.py` — это не файл с
ответственностями, это **одна функция на 423 строки**, которой понадобилось
22 поля состояния.

### Л3. Состояние прогона названо, но не инкапсулировано

`AttemptState` (`core/loop_attempt.py:45-109`) — 22 поля, созданные явно, потому
что `ruff.toml` этого репозитория ставит `max-args = 12`, а функции требовалось
22 аргумента. Это честный шаг, и он записан в докстроке файла. Но он назвал
проблему, а не убрал: поля делятся на «вход», «рабочее» и «выход» комментариями,
а не типами, и `OUTPUTS` (`:100-109`) — множество строк, проверяемое тестом, а не
структура, которую нельзя собрать неправильно.

**Гипотеза (не проверена):** три группы полей — это три разные роли, которые
хочет иметь три разных объекта; проверяется чтением `_run_inner`, который
`AttemptState` создаёт и распаковывает.

### Л4. Часть слоя нарезана инструментом, режущим по бюджету строк

Не гипотеза: `core/incremental_splitter.py` существует, и его собственная
докстрока описывает **«mixin mode»** —

> «когда файл занят одним огромным классом (**случай `AgentLoop`**), переносит
> самодостаточные методы в класс-примесь в новом соседнем модуле и добавляет
> примесь в базы класса»

с бюджетом `DEFAULT_MAX_MOVE_LINES = 400` (`:49`) и порогом «класс доминирует,
если владеет ≥ 50 % строк модуля» (`_DOMINANT_CLASS_RATIO = 0.5`, `:53`).

Критерий переноса — **строки и самодостаточность по именам**, а не связность по
смыслу. Семь файлов `core/` носят в докстроке след этого происхождения, и все
семь говорят о нём одинаково:

> «`core/loop_methods.py`, откуда это приехало, не было модулем: его сделал
> `core/incremental_splitter.py`, резавший `core/loop.py` по бюджету строк, а не
> по смыслу. Имя `methods` — это «остальное»…»

Это механизм, объясняющий Л1: примеси появились не как решение о границах, а как
единственная операция, которой инструмент мог уменьшить файл, не сломав
`self.`-доступ. Инструмент честен — он и не обещал границ по смыслу. Но результат
его работы сейчас читается как архитектура.

### Л5. Докстрока о вызывающих сторонах — тоже не доказательство

`core/loop_memory_commands.py:8-11` утверждает: «Это НЕ код цикла. Ни
`_run_inner`, ни один из его этапов сюда не заходит — зовут отсюда CLI и
самопочинка».

Проверка вызовов **опровергает половину утверждения**. `forget` и
`list_persistent` — действительно только CLI (`cli/command_dispatch.py:213,216`,
`cli/commands_memory.py:184`). Но `remember` достижим изнутри цикла через одну
косвенность:

```
loop_evidence_chain.py:261  remember=self._knowledge_remember_batch() …
loop_verify_replan.py:459   self._knowledge_remember_batch()
        └─ loop_knowledge.py:78  return self.remember(…)
                └─ loop_memory_commands.py:43  def remember(…)
```

То есть конвейер знаний хода пишет в долгую память **через этот файл, каждый
цикл**. Правило «имя файла — не доказательство» распространяется и на прозу
внутри него: проверять надо вызовы.

---

## Записи по файлам

### `core/loop_attempt.py`

- **Путь:** `core/loop_attempt.py` — 652 строки.
- **Текущее назначение (по докстроке):** «Цикл попыток: план → исполнение →
  вердикт → перепланирование». Десятый кусок раскола `core/loop.py`.

**Фактические ответственности** — одиннадцать, из них девять внутри одного
метода `_run_attempt_loop`:

| # | ответственность | символы и строки |
|---|---|---|
| 1 | Управление циклом попыток: счётчик, решение продолжать, исчерпание | `_run_attempt_loop:149-151`, `:461-472`, `:497-526` |
| 2 | Сборка контекста перепланирования и перенос совета/запретов между попытками | `:171-177`, `:524-525` |
| 3 | **Добыча плана пятью разными путями** | см. ниже |
| 3а | — принудительный план от вызывающего | `:179-185` |
| 3б | — пропуск планировщика на пути локальной критики | `:190-220` |
| 3в | — пропуск планировщика на «дешёвом пути» | `:228-255` |
| 3г | — кэш планировщика по (вопрос, mtime хранилища эпизодов, file_hint) | `:256-280` |
| 3д | — собственно вызов планировщика | `:281-300` |
| 4 | Чекпойнт при исчерпании бюджета модели | `:290-300`, `_save_budget_pause_checkpoint:604-652` |
| 5 | Отчёт о галлюцинациях планировщика (`plan_tool_drop`) | `:330-338` |
| 6 | Сенсор «рассуждение ↔ действие» | `:340-360` |
| 7 | Сенсор застоя + теневой учёт ранней остановки | `:474-495` |
| 8 | Ворота на непарсящийся план | `:399-428` |
| 9 | Извлечение посылок из плана (Layer 5) | `:366-384` |
| 10 | Оркестрация исполнения шагов и сборка цепочки улик | `:430-459` |
| 11 | Построение плана из источников | `_build_plan:566-583` |
| 12 | Сводки шагов для чекпойнта | `_checkpoint_step_summaries:586-602` |

- **Входящие зависимости:** `core/loop.py` (единственная).
- **Исходящие зависимости:** `core.assumption_registry`, `core.evidence`,
  `core.file_request_intent`, `core.model_usage`, `core.models`, `core.planner`,
  `core.reasoning_action_check`, `core.replan`, `core.task_complexity` — плюс
  **13 атрибутов чужих примесей**, объявленных в `:120-140` и не создаваемых
  здесь: `log`, `planner`, `replan_policy`, `_current_attempt`,
  `_termination_guard`, `_execute_steps_parallel`, `_sensor_failed`,
  `_defect_signals`, `_episodic_store_mtime`, `_executed_tools`,
  `_planner_cache`, `cheap_path_enabled`, `last_referent_decision`.
- **Состояние:** `AttemptState` — 22 поля (`:45-109`); плюс мутация
  `self._current_attempt`, `self._defect_signals`, `self._planner_cache`.
- **Побочные эффекты:** 6 точек записи в журнал прогона; запись чекпойнтов через
  `st._cp` (`save_plan:364`, `save_act:454-459`, пауза по бюджету `:604-652`).
  Файловых операций и порождения процессов нет.
- **Связанные тесты:** `tests/test_loop_attempt_split.py` (сверка AST переноса с
  историей), `tests/test_loop_split_wiring.py`, `tests/test_loop_small_methods_split.py`,
  `tests/test_completion_contract.py`, `tests/test_plan_parse_failed_gate.py`.

**Архитектурные нарушения**

1. **Две несвязанные ответственности в одном теле.** «Откуда взять план» (пять
   взаимоисключающих путей, 120 строк) и «что делать с результатом попытки»
   (решение о перепланировании, 100 строк) не разделяют ни состояния, ни
   причины меняться. Первая меняется при появлении нового способа получить план,
   вторая — при изменении политики повторов.
2. **Три сенсора живут внутри цикла управления.** Проверка рассуждение↔действие,
   сторож застоя и ворота непарсящегося плана вставлены в поток управления
   точечными блоками `try/except`, каждый со своим правилом «наблюдательный,
   ход не ломает». Место сенсора определяется не его смыслом, а тем, в какой
   момент цикла оказались нужные локальные переменные.
3. **Пустой план имеет три разных значения**, и каждое пришлось добавлять в одно
   условие после того, как предыдущее его проглотило (`:461-465`): «планировщик
   выбрал ноль инструментов» (успех), «JSON не разобрался» (провал). Условие
   растёт линейно с числом причин — форма, которая по построению принимает
   следующую причину молча.
4. **Кэш планировщика — чужая подсистема.** Ключ, инвалидация по mtime хранилища
   эпизодов и хранение (`:256-280`) не имеют отношения к управлению попытками.

- **Предлагаемое целевое назначение:** только управление циклом попыток —
  счётчик, вызов политики перепланирования, перенос совета и запретов, вердикт
  об исчерпании. Всё остальное — соседи.
- **Предлагаемое имя:** `core/attempt_control.py` для остатка;
  добыча плана выделяется в `core/plan_acquisition.py` (пять путей + кэш),
  сенсоры — в существующий `core/loop_sensor.py`.
- **Классификация:** **overloaded**.
- **Рекомендуемое действие:** разделить после подтверждения; первым кандидатом —
  добыча плана, потому что у неё нет общего состояния с решением о повторе
  (проверяемо: пути 3а–3д пишут только `st.planner_out` и `st.cheap_path_active`).

---

### `core/loop.py`

- **Путь:** `core/loop.py` — 735 строк.
- **Текущее назначение:** сборка класса `AgentLoop` из 19 примесей и тело
  основного цикла.

**Фактические ответственности**

| # | ответственность | символы и строки |
|---|---|---|
| 1 | Композиция класса из 19 примесей | `class AgentLoop:143` + 19 импортов `core.loop_*` |
| 2 | Публичная точка входа одного хода | `run:185-234` |
| 3 | Ворота быстрого пути / реиграния ответа | `_fast_path_allows_replay:244-260`, `_quality_allows_replay:263-274` |
| 4 | **Тело цикла целиком** | `_run_inner:276-692` — **417 строк** |
| 5 | Определение корня рабочей области для чтения файлов | `_file_read_workspace_root:718-726` |

- **Входящие зависимости:** 29 файлов вне `core/` — весь `cli/`, `app/`,
  `scripts/completion_legacy_report.py`, `core/subagent_runner.py`. Это
  **фактический публичный интерфейс агента**.
- **Исходящие зависимости:** 35 модулей, из них 19 — собственные примеси.
- **Побочные эффекты:** 2 точки записи в журнал; остальное делегировано.
- **Связанные тесты:** свыше 80 тестовых файлов импортируют `core.loop`.

**Архитектурные нарушения**

1. **Файл совмещает роль фасада и роль реализации.** Композиция примесей
   (`:143`) — это сборка; `_run_inner` на 417 строк — это самая крупная
   реализация в слое. Импортёров у файла 29, и каждый из них зависит от обоих.
2. **`_run_inner` — единственное место, где виден порядок фаз хода.** Гипотеза
   (не проверена чтением всех 417 строк): именно поэтому все попытки вынести
   куски заканчивались примесями — фазы связаны локальными переменными, а не
   переданным состоянием.

- **Предлагаемое целевое назначение:** только композиция и публичный вход.
- **Предлагаемое имя:** имя корректно для фасада (`core/loop.py`); тело цикла
  просится в `core/loop_cycle.py`.
- **Классификация:** **overloaded** (фасад + крупнейшая реализация слоя).
- **Рекомендуемое действие:** не трогать до инвентаризации остальных 17 файлов —
  у него 29 внешних импортёров, и любое движение здесь дороже всего в слое.

---

### `core/loop_step_execution.py`

- **Путь:** `core/loop_step_execution.py` — 911 строк, самый большой в слое.
- **Текущее назначение:** исполнение шагов плана.

**Фактические ответственности**

| # | ответственность | символы и строки |
|---|---|---|
| 1 | Параллельное исполнение независимых шагов | `_execute_steps_parallel:138-210`, `_run_step_parallel:99-112` |
| 2 | Классификация шага как read-only | `_step_only_reads:114-136` |
| 3 | **Исполнение одного шага: Act → Policy → Tool → Observe** | `_execute_step:212-769` — **558 строк** |
| 4 | Вызов инструмента | `_call_tool:771-796` |
| 5 | Извлечение компенсирующего плана из вывода инструмента | `_capture_compensation_plan:798-829` |
| 6 | Мост к запросу одобрения человеком | `_request_approval:832-911` |

- **Входящие зависимости:** `core/loop.py` (единственная).
- **Исходящие зависимости:** `core.actuation_gateway`, `core.budget_kill_switch`,
  `core.compensation`, `core.data_classifier`, `core.injection_guard`,
  `core.models`, `core.redaction`, `core.replan`, `core.tool_receipts`.
- **Побочные эффекты:** 6 точек записи в журнал; исполнение инструментов —
  единственное место в слое, где происходят внешние действия.
- **Связанные тесты:** `tests/test_loop_step_execution_split.py`,
  `tests/test_integration.py`.

**Архитектурные нарушения**

1. **`_execute_step` на 558 строк — самая крупная функция репозитория.** Она
   проходит через шлюз воздействия, убийственный выключатель бюджета,
   классификатор данных, защиту от инъекций, редактирование секретов, квитанции
   инструментов и одобрение — семь подсистем в одном теле.
   **Гипотеза (файл не прочитан целиком):** это последовательность независимых
   ворот, каждые из которых можно назвать и вынести; проверяется чтением
   `:212-769` целиком.

- **Предлагаемое целевое назначение:** оркестрация исполнения шага; сами ворота —
  отдельными проверяемыми единицами.
- **Предлагаемое имя:** имя корректно (`loop_step_execution`), вопрос к телу.
- **Классификация:** **overloaded**.
- **Рекомендуемое действие:** прочитать `_execute_step` целиком до любых
  предложений — 558 строк не оценены, и оценивать их по имени функции нельзя.

---

### `core/loop_sensor.py`

- **Путь:** `core/loop_sensor.py` — 44 строки. **Прочитан целиком.**
- **Текущее назначение:** запись о сбое наблюдательного сенсора.
- **Фактические ответственности:** одна. `AgentLoopSensor._sensor_failed:38-44`
  делегирует в `core.sensor_journal.journal_sensor_failure`. Тела логики нет.
- **Входящие:** `core/loop.py`. Но контракт метода **объявляют пять примесей
  цикла** — самый разделяемый метод раскола; это и есть причина, по которой у
  него отдельный адрес, а не место в общем мешке.
- **Исходящие:** `core.sensor_journal`.
- **Побочные эффекты:** нет собственных; запись в журнал происходит в
  делегате.
- **Связанные тесты:** `tests/test_loop_split_wiring.py` (проводка примеси);
  поведение сенсорного журнала покрыто отдельно.
- **Архитектурные нарушения:** не обнаружено.
- **Предлагаемое целевое назначение:** без изменений.
- **Предлагаемое имя:** `core/loop_sensor.py` — соответствует телу.
- **Классификация:** **cohesive**.
- **Рекомендуемое действие:** не трогать. Это контрпример к оценке по размеру:
  44 строки и один метод — не признак лишнего файла, когда у метода пять
  объявителей.

---

### `core/loop_knowledge.py`

- **Путь:** `core/loop_knowledge.py` — 87 строк. **Прочитан целиком.**
- **Текущее назначение (по докстроке):** «Запись знаний, добытых конвейером, в
  долгую память».
- **Фактические ответственности:** две, и они разной природы.

| # | ответственность | символы и строки |
|---|---|---|
| 1 | Переходник имени: `_remember_from_knowledge` — сквозная передача пяти аргументов в `self.remember` без единого решения | `:39-53` |
| 2 | Оптимизация: замыкание, загружающее снимок хранилища ОДИН раз на проход конвейера вместо перезагрузки на каждую запись (TD-019) | `_knowledge_remember_batch:55-87` |

- **Входящие:** `core/loop.py`; фактические потребители —
  `core/ingestion.py:210,335,516` через `getattr(agent, "_remember_from_knowledge", None)`
  и `core/loop_evidence_chain.py:261`, `core/loop_verify_replan.py:459`.
- **Исходящие:** `core.knowledge_pipeline`, `core.memory_policy`, `core.models`.
- **Побочные эффекты:** нет прямых; запись делает `remember` ниже по цепочке.
- **Связанные тесты:** `tests/test_loop_split_wiring.py`; поведение записи —
  `tests/test_unattended_memory_writes.py`.
- **Архитектурные нарушения:**
  1. **Переходник существует ради динамического поиска по имени.**
     `core/ingestion.py` берёт метод через `getattr` по строке, то есть связь
     между конвейером приёма и циклом держится на совпадении имени и не
     проверяется ни импортом, ни типом. Переименование метода здесь не сломает
     ни один импорт — оно молча выключит запись знаний.
  2. **Две ответственности разной скорости изменения.** Имя-переходник меняется
     вместе с контрактом конвейера; кэш снимка — вместе с профилем
     производительности хранилища.
- **Предлагаемое целевое назначение:** только адаптер конвейера знаний к
  политике записи памяти.
- **Предлагаемое имя:** `core/knowledge_memory_adapter.py`.
- **Классификация:** **compatibility facade** (пункт 1) с примесью
  **overloaded** (пункт 2).
- **Рекомендуемое действие:** не переносить. Сначала заменить `getattr`-шов на
  явный протокол — иначе перенос файла тихо оборвёт запись знаний, и ни один
  тест импорта этого не заметит. **Гипотеза:** тесты, покрывающие этот шов,
  проверяют результат конвейера, а не наличие метода; проверяется чтением
  `tests/test_unattended_memory_writes.py` и тестов приёма.

---

### `core/loop_memory_commands.py`

- **Путь:** `core/loop_memory_commands.py` — 191 строка. **Прочитан целиком.**
- **Текущее назначение (по докстроке):** «Операторские команды памяти:
  запомнить, забыть, показать».
- **Фактические ответственности**

| # | ответственность | символы и строки |
|---|---|---|
| 1 | Запись в долгую память через политику: загрузка снимка для дедупликации, антидот эха, решение политики | `remember:43-97` |
| 2 | Редакция секретов и ПДн перед сохранением + событие `sensitive_detected` | `:112-122` |
| 3 | Сохранение записи и поддержание переданного снимка в актуальном состоянии | `:124-139` |
| 4 | Ведение журнала эха для агентских автозаписей | `:143-158` |
| 5 | Удаление записей | `forget:173-186` |
| 6 | Чтение всех записей | `list_persistent:188-191` |

- **Входящие:** `core/loop.py`. Фактические вызывающие: `cli/command_dispatch.py:213,216`,
  `cli/commands_memory.py:184`, `core/autonomous_runtime.py:850` — **и цикл**,
  через `_knowledge_remember_batch` (см. Л5).
- **Исходящие:** `core.memory_echo_antibody`, `core.memory_policy`,
  `core.models`, `core.redaction`.
- **Побочные эффекты:** запись и удаление в постоянном хранилище; 6 точек
  журнала; запись в реестр эха.
- **Связанные тесты:** `tests/test_unattended_memory_writes.py`,
  `tests/test_persistent_integration.py`, тесты политики записи.
- **Архитектурные нарушения:**
  1. **Докстрока неверна о вызывающих** (Л5) — и это не косметика: она
     обосновывает вывод «файл misplaced», который на её основании был бы ошибкой.
  2. **Три подсистемы в одном методе.** `remember:43-171` совмещает решение
     политики, редакцию ПДн и ведение журнала эха. Каждая имеет собственный
     модуль (`memory_policy`, `redaction`, `memory_echo_antibody`) — здесь они
     сшиты последовательностью, а не составлены.
  3. **Операторские команды и внутренняя запись — один вход.** `remember`
     обслуживает и явное распоряжение человека, и автоматическую запись
     конвейера, различая их только значением аргумента `source`.
- **Предлагаемое целевое назначение:** единственная точка записи в долгую
  память со всеми воротами; операторские `forget`/`list_persistent` — отдельно.
- **Предлагаемое имя:** `core/persistent_memory_write.py` для `remember`;
  `forget`/`list_persistent` просятся в операторский слой (`cli/`), поскольку
  их вызывающие — только он.
- **Классификация:** **overloaded**; для `forget`/`list_persistent` —
  **misplaced**.
- **Рекомендуемое действие:** не переносить до конца инвентаризации. Разрез
  проходит не по «команды оператора / не команды», как утверждает докстрока, а
  по «запись через ворота / чтение и удаление без ворот».

---

## Остальные 14 файлов слоя — структурные факты

Файлы **ещё не прочитаны целиком**. Ниже только машинные факты; классификация
для них не выставляется, любое суждение было бы оценкой по размеру и имени —
ровно то, что запрещено заданием.

| файл | строк | входящие | исходящие | побочные эффекты |
|---|---|---|---|---|
| `loop_context.py` | 286 | `core/loop.py` | 5 | журнал ×5 |
| `loop_evidence_chain.py` | 272 | `core/loop.py` | 2 | журнал ×6 |
| `loop_gates.py` | 259 | `core/loop.py` | 2 | журнал ×6 |
| `loop_hygiene.py` | 310 | `core/loop.py` | 2 | журнал ×6 |
| `loop_init.py` | 301 | `core/loop.py` | **29** | нет |
| `loop_memory_read.py` | 444 | `core/loop.py` | 3 | журнал ×6 |
| `loop_memory_write.py` | 456 | `core/loop.py` | 3 | журнал ×6 |
| `loop_observe.py` | 204 | `core/loop.py` | 4 | журнал ×6 |
| `loop_repair.py` | 215 | `core/loop.py` | 6 | чтение файла ×1, журнал ×5 |
| `loop_response_deciders.py` | 322 | `core/loop.py` | 7 | журнал ×6 |
| `loop_run_tail.py` | 333 | `core/loop.py` | 2 | журнал ×6 |
| `loop_synthesis.py` | 664 | `core/loop.py` | **17** | журнал ×5 |
| `loop_verification.py` | 234 | `core/loop.py` | 10 | журнал ×6 |
| `loop_verify_replan.py` | 508 | `core/loop.py` | 8 | журнал ×6 |

Два места, куда смотреть следующими, и почему именно туда:

- **`loop_init.py`** — 301 строка и **29 исходящих зависимостей**, больше, чем у
  любого другого куска слоя, при нуле побочных эффектов. Гипотеза: это не
  «инициализация цикла», а сборка всего графа объектов агента, то есть роль
  композиционного корня, оказавшаяся внутри примеси.
- **`loop_memory_read.py` + `loop_memory_write.py`** — 900 строк на двоих,
  разделены по направлению операции, а не по подсистеме. Гипотеза: чтение и
  запись памяти делят инварианты (что считается пригодным эпизодом), и разрез по
  «читает/пишет» проходит поперёк них — та же форма, что заставила
  `decide_usage_eligibility` и `loop_memory_read.py:230-240` независимо повторять
  правило про тег `lesson`.

---

## Статус прохода

- **Прочитаны целиком (4):** `loop_attempt.py`, `loop_sensor.py`,
  `loop_knowledge.py`, `loop_memory_commands.py`.
- **Прочитаны частично (2):** `loop.py`, `loop_step_execution.py` — записи
  опираются на символьную карту и прочитанные фрагменты; тела `_run_inner`
  (417 строк) и `_execute_step` (558 строк) не прочитаны целиком, и выводы о них
  помечены как гипотезы.
- **Не прочитаны (14):** таблица выше содержит только машинные факты, без
  классификации.
- **Инвентаризация слоя не завершена. Перенос, переименование и удаление не
  начинаются.**

### Что уже видно на четырёх прочитанных

Классификации распределились так, что оценка по размеру предсказала бы их
неверно в обе стороны:

| файл | строк | классификация |
|---|---|---|
| `loop_sensor.py` | 44 | **cohesive** — самый маленький, и трогать нечего |
| `loop_knowledge.py` | 87 | **compatibility facade** + overloaded |
| `loop_memory_commands.py` | 191 | **overloaded**, частью **misplaced** |
| `loop_attempt.py` | 652 | **overloaded** |

Самый маленький файл слоя оказался единственным пока связным; 87-строчный —
переходником, держащимся на совпадении строки в `getattr`.
